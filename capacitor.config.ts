import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.angular.example',
  appName: 'ionic-angular-example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
